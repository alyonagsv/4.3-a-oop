#pragma once
#include "vector"
#include "Student.h"
using namespace std;
class Student_bd
{
    vector<Student> students;
public:
    Student_bd();        // ??????????? ??? ??????????
    Student_bd(const vector<Student>& students);  // ??????????? ? ??????????? (????????? ?????? ?????????) 
    Student_bd(const Student_bd& other);        // ??????????? ???????????
    Student_bd(Student_bd&& other) noexcept;        // ??????????? ???????????
    ~Student_bd() {};

    void addStudent();
    void loadDatabaseBinary(const char* filename);
    void saveDatabaseBinary(const char* filename);
    void print();
    void calculateAverageAge(int course);
    void searchStudents(const string& name_search);
    //void searchCharStudents( char* name_search);
};
